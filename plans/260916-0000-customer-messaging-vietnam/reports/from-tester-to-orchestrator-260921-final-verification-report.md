# Final Verification — Zalo OA Chat — 2026-09-21

Repo `/Users/luan/repos/crm`, branch `feat/zalo-oa-chat`. No source file edited.

## Commands

| # | Command | Result | Exit |
|---|---|---|---|
| 1 | `bun run check-types` | 13/13 tasks successful (11 cached). `apps/api/src/generated/server.ts` regenerated by `api:trpc:generate` task, now shows modified in git status. | 0 |
| 2 | `bun run lint` | 9/9 tasks successful, 0 errors, 4 warnings total (list below) | 0 |
| 3 | `bun run i18n:check` | 0 findings, 0 ratcheted. Scanned 344 files, 50 allowlisted | 0 |
| 3b | `bun run i18n:catalogs` | catalogs in sync for vi, 1 warning: `settings.connections.alreadyConnectedList` does not use `{count}` | 0 |
| 4 | `apps/app: bun test` | 209 pass, 0 fail, 477 expect() calls, 19 files | 0 |
| 5 | `apps/api: bun test --preload ./test/setup.ts` | 481 pass, 0 fail, 1104 expect() calls, 39 files. Log noise is expected: tests intentionally exercise Zalo timeout/rate-limit/invalid-token and unconnected-OA and store-failure paths | 0 |
| 6 | `apps/agent: bun test` | 395 pass, 0 fail, 979 expect() calls, 34 files. Log noise is expected: tests intentionally exercise Zalo timeout/rate-limit/invalid-token paths | 0 |
| 7 | `packages/db: bun test` | 133 pass, 0 fail, 10 files | 0 |
| 7b | `packages/telemetry: bun test` | 67 pass, 0 fail, 5 files | 0 |
| 7c | `packages/validation: bun test` | 5 pass, 0 fail, 1 file | 0 |
| 7d | `packages/auth: bun test` | 43 pass, 0 fail, 5 files. One test intentionally throws inside a handler to prove it does not fail sign-in; logged error is expected | 0 |
| 7e | `packages/ui: bun test` | No `test` script in package.json. Skipped | n/a |
| 8 | `packages/db: bunx prisma migrate status` (dev, .env `DATABASE_URL`) | "Database schema is up to date!" against DB `crm`. 54 migrations found | 0 |
| 8b | same with `DATABASE_URL=$TEST_DATABASE_URL` | "Database schema is up to date!" against DB `crm_test`. 54 migrations found | 0 |
| 8c | migration files present | `20260921191500_messaging` and `20260921221500_messaging_profile_checked` both exist in `prisma/migrations` and are applied in both DBs (status "up to date" covers all 54) | — |

## Lint warnings (file:line)

1. `packages/ui/src/styles/globals.css:881` — `noImportantStyles`, pre-existing, not Zalo-feature code.
2. `packages/ui/src/styles/globals.css:882` — `noImportantStyles`, pre-existing, not Zalo-feature code.
3. `apps/app/components/crm/messaging/message-bubble.tsx:139` — `noImgElement`, plain `<img>` used for Zalo image attachments instead of `next/image`.
4. `apps/app/lib/locale.ts:8` — `noDocumentCookie`, direct `document.cookie` write, pre-existing locale code.

## Static grep checks

- `grep -rn "message/cs" apps/api/src` — empty. Pass.
- `grep -rn "console.log" apps/api/src/messaging` — empty. Pass.
- `grep -rn "Co-Authored" .githooks` — empty (no match / no hook file). Info only.
- `grep -rln "accessToken\|refreshToken"` across `apps/api/src` and `apps/agent/agent` — 21 files hit. Only 2 do a real Prisma **select** of Zalo token columns:
  - `apps/api/src/messaging/zalo/zalo-oauth.service.ts` — expected.
  - `apps/agent/agent/lib/messaging/zalo-connection.ts` — expected.
  Every other hit checked individually:
  - `apps/api/src/messaging/zalo/zalo-connection.service.ts:98-99` — `updateMany` **writing** `accessToken: null, refreshToken: null` on disconnect. Not a select.
  - `apps/api/src/conversations/conversations.service.ts:221` — `db.account.findFirst({ where: { providerId: "slack", accessToken: { not: null } }, select: { id: true } })`. Slack, pre-existing, selects `id` only.
  - `apps/agent/agent/lib/messaging/message-identity-profile.ts`, `message-send.ts`, `message-token-refresh.ts`, `zalo-client.ts` — read `account.accessToken`/`refreshToken` from an object passed in, or declare it as a function-param/interface field. No `prisma.`/`select:`/`findFirst`/`findUnique` in these files.
  - `apps/agent/agent/lib/builder-runtime.ts`, `run-runtime.ts`, `slack-connection.ts` (real `select: { accessToken: true }` at line 7), `slack-people.ts` — all Slack, pre-existing, unrelated to Zalo.
  - Google (`calendar-sync.service.ts`, `calendar.client.ts`, `gmail-sync.service.ts`, `gmail.client.ts`), Microsoft (`graph.client.ts`, `outlook-sync.service.ts`), mailbox (`mailbox-api.client.ts`, `mailbox-token.service.ts`) — other providers, pre-existing, unrelated to Zalo.

## Capability off-switch (no throw when Zalo unset)

- `apps/api/src/messaging/messaging-config.ts` exports `isZaloConfigured()`. With `ZALO_APP_ID=` `ZALO_APP_SECRET=`: returns `false`, no throw. Exit 0.
- `apps/agent/agent/lib/messaging/zalo-connection.ts` exports `isZaloConfigured()` (same name). Same env, returns `false`, no throw. Exit 0.

## Git status (for the record)

- `git status --short | grep -v '^??' | grep -v '.agents/'` — 60 tracked changes (list in full report run; mix of `M` source/doc/i18n files and `AM`/`A` plan+report files under `plans/260916-0000-customer-messaging-vietnam/`). No unexpected files outside the feature scope.
- Untracked file count under `apps/`, `packages/`, `docs/`, `plans/` matching filter: 56.

## Issues

None.

Status: DONE

Summary: All 13 check-types tasks pass. Lint 0 errors, 4 pre-existing/minor warnings.
i18n check and catalogs clean (1 non-blocking count-format warning). All 6 test
suites green: app 209, api 481, agent 395, db 133, telemetry 67, validation 5,
auth 43 — 1333 tests, 0 fail. Both migrations applied in dev and test DB. Static
acceptance greps clean: no `message/cs`, no stray console.log in messaging, token
columns selected only in the two expected files. Zalo capability degrades to
`false` with no throw when unset.

## Unresolved questions

1. `message-bubble.tsx:139` uses `<img>` not `next/image` — pre-existing lint
   warning, not a new regression from this pass but flagged since it is in
   Zalo-feature code. Orchestrator to decide if it needs a fix before merge.
2. `packages/ui` has no `test` script — confirm this is intentional before
   final release sign-off.
